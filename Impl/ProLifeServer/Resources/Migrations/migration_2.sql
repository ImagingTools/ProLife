CREATE TABLE "Devices"(
    DeviceId VARCHAR (1000) NOT NULL,
    DeviceName VARCHAR (1000) NOT NULL, 
    MacAddress VARCHAR (1000) NOT NULL,
    SerialNumber VARCHAR (1000) NOT NULL,
    PRIMARY KEY (DeviceId)
);
