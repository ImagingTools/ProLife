node_modules/
npm-debug.log*
.auth/
# Everything a run writes: test-output/<phase>/{artifacts,junit.xml}, and test-output/artifacts
# for an ad hoc `npx playwright test` that sets no phase of its own.
test-output/
